@using AlgoaBayBMT.Services.Models
@using Microsoft.AspNetCore.Components.Forms
@using Syncfusion.Blazor.Buttons
@using Syncfusion.Blazor.Inputs

<SfCard CssClass="pf-card-shell pf-training-editor-card">
    <CardHeader Title="Module Editor" SubTitle="Create, update, and reorder modules within the active course version." />
    <CardContent>
        <EditForm Model="Model" OnValidSubmit="HandleSaveAsync" FormName="@GetFormName()">
            <DataAnnotationsValidator />
            <ValidationSummary class="validation-message" />

            <div class="pf-form-grid">
                <div class="pf-col-12 pf-col-title-wide">
                    <label class="pf-field-label">Title</label>
                    <SfTextBox @bind-Value="Model.Title" Placeholder="Module title" FloatLabelType="FloatLabelType.Never" />
                </div>
                <div class="pf-col-12 pf-col-minutes-compact">
                    <label class="pf-field-label">Estimated Minutes</label>
                    <InputNumber @bind-Value="Model.EstimatedMinutes" class="form-control" />
                </div>
                <div class="pf-col-12">
                    <label class="pf-field-label">Description</label>
                    <InputTextArea @bind-Value="Model.Description" class="form-control pf-textarea" />
                </div>
                <div class="pf-col-12">
                    <label class="pf-inline-check">
                        <InputCheckbox @bind-Value="Model.IsActive" />
                        <span>Module active</span>
                    </label>
                </div>
            </div>

            <div class="pf-dialog-actions pf-dialog-actions--split">
                <div class="pf-grid-actions pf-grid-actions--compact">
                    <SfButton type="button" OnClick="MoveUpAsync">Move Up</SfButton>
                    <SfButton type="button" OnClick="MoveDownAsync">Move Down</SfButton>
                    @if (Model.ModuleId.HasValue)
                    {
                        <SfButton type="button" CssClass="e-danger" OnClick="DeleteAsync">Delete</SfButton>
                    }
                </div>
                <div class="pf-grid-actions pf-grid-actions--compact">
                    @if (Model.ModuleId.HasValue)
                    {
                        <SfButton type="button" CssClass="e-primary" OnClick="CreateLessonAsync">New Lesson</SfButton>
                    }
                    <SfButton CssClass="e-primary" IsPrimary="true" type="submit">Save Module</SfButton>
                </div>
            </div>
        </EditForm>
    </CardContent>
</SfCard>

@code {
    [Parameter, EditorRequired]
    public TrainingModuleEditModel Model { get; set; } = new();

    [Parameter]
    public EventCallback<TrainingModuleEditModel> SaveRequested { get; set; }

    [Parameter]
    public EventCallback DeleteRequested { get; set; }

    [Parameter]
    public EventCallback<int> MoveRequested { get; set; }

    [Parameter]
    public EventCallback CreateLessonRequested { get; set; }

    private string GetFormName() => Model.ModuleId.HasValue
        ? $"training-module-{Model.ModuleId.Value:N}"
        : $"training-module-new-{System.Runtime.CompilerServices.RuntimeHelpers.GetHashCode(Model)}";

    private async Task HandleSaveAsync() => await SaveRequested.InvokeAsync(Model);
    private async Task DeleteAsync() => await DeleteRequested.InvokeAsync();
    private async Task MoveUpAsync() => await MoveRequested.InvokeAsync(-1);
    private async Task MoveDownAsync() => await MoveRequested.InvokeAsync(1);
    private async Task CreateLessonAsync() => await CreateLessonRequested.InvokeAsync();
}
